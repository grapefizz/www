{
	"buildCommand": "npm run build",
	"devCommand": "npm run dev",
	"installCommand": "npm install --legacy-peer-deps",
	"framework": "sveltekit",
	"rewrites": [
		{
			"source": "/stats/:match*",
			"destination": "https://us.umami.is/:match*"
		}
	],
	"git": {
		"deploymentEnabled": false
	}
}
